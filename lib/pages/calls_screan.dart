import 'package:flutter/material.dart';

class CallScrean extends StatelessWidget {
  const CallScrean({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Calls",
        style: TextStyle(fontSize: 20.0),
      ),
    );
  }
}
